generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model Fund {
  id          Int    @id @default(autoincrement())
  manager     String
  address     String
  description String
}
